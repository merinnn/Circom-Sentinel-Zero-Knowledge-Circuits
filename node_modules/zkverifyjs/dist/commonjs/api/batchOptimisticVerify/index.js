"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.batchOptimisticVerify = void 0;
const optimisticVerify_1 = require("../optimisticVerify");
const batchOptimisticVerify = async (connection, options, input) => {
    for (let i = 0; i < input.length; i++) {
        const res = await (0, optimisticVerify_1.optimisticVerify)(connection, options, input[i]);
        if (!res.success) {
            return {
                success: false,
                type: res.type,
                message: `Proof at index ${i} failed: ${res.message}`,
                code: res.code,
                verificationError: res.verificationError,
                failedIndex: i,
            };
        }
    }
    return {
        success: true,
        type: 'ok',
        message: 'Optimistic Verification Successful!',
    };
};
exports.batchOptimisticVerify = batchOptimisticVerify;
