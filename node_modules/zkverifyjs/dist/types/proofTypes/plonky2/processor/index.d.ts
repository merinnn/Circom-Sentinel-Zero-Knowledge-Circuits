import { ProofProcessor } from '../../../types';
import { Plonky2Proof, Plonky2Vk, Plonky2Pubs } from '../types';
import { ProofOptions } from '../../../config';
declare class Plonky2Processor implements ProofProcessor {
    formatProof(proof: Plonky2Proof['proof'], options: ProofOptions): {
        bytes: string;
    };
    formatVk(vk: Plonky2Vk['vk'], options: ProofOptions): {
        config: string;
        bytes: string;
    };
    formatPubs(pubs: Plonky2Pubs['pubs'], options: ProofOptions): string;
}
declare const _default: Plonky2Processor;
export default _default;
//# sourceMappingURL=index.d.ts.map