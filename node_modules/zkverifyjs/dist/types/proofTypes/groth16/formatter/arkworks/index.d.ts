import { Groth16VerificationKey, Proof, ProofInput } from '../../types';
import { ProofOptions } from '../../../../config';
/**
 * Arkworks VK format already matches Groth16VerificationKey shape.
 */
type ArkworksVerificationKey = Groth16VerificationKey;
/**
 * Returns proof as-is, just extracts fields.
 */
export declare const formatProof: (proof: ProofInput, options: ProofOptions) => Proof;
/**
 * Returns VK as-is, trusting Arkworks formatting is already compatible.
 */
export declare const formatVk: (vk: ArkworksVerificationKey, options: ProofOptions) => Groth16VerificationKey;
/**
 * Arkworks public signals require no transformation.
 */
export declare const formatPubs: (pubs: string[]) => string[];
declare const _default: {
    formatProof: (proof: ProofInput, options: ProofOptions) => Proof;
    formatVk: (vk: ArkworksVerificationKey, options: ProofOptions) => Groth16VerificationKey;
    formatPubs: (pubs: string[]) => string[];
};
export default _default;
//# sourceMappingURL=index.d.ts.map