import { VerifyInput } from '../../../api/verify/types';
import { ProofOptions } from '../../../config';
import { OptimisticVerifyOptions } from '../../types';
import { OptimisticVerifyResult } from '../../../types';
export declare class BatchOptimisticVerificationBuilder {
    private readonly executeBatchOptimisticVerify;
    private readonly options;
    private nonceSet;
    private registeredVkSet;
    private blockSet;
    constructor(executeBatchOptimisticVerify: (options: OptimisticVerifyOptions, input: VerifyInput[]) => Promise<OptimisticVerifyResult>, proofOptions: ProofOptions, accountAddress?: string);
    nonce(nonce: number): this;
    withRegisteredVk(): this;
    atBlock(block: number | string): this;
    execute(input: VerifyInput[]): Promise<OptimisticVerifyResult>;
}
//# sourceMappingURL=index.d.ts.map