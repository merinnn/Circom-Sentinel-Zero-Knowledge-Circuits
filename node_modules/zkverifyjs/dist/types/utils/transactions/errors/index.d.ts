import { ApiPromise, SubmittableResult } from '@polkadot/api';
import { EventEmitter } from 'events';
import { TransactionInfo } from '../../../types';
import { DispatchError } from '@polkadot/types/interfaces';
export declare const decodeDispatchError: (api: ApiPromise, err: DispatchError) => {
    code?: string;
    message: string;
    section?: string;
};
export declare const handleError: <T extends TransactionInfo>(emitter: EventEmitter, api: ApiPromise, transactionInfo: T, error: unknown, shouldThrow?: boolean, status?: SubmittableResult["status"]) => void | never;
//# sourceMappingURL=index.d.ts.map